*[_type == $type] | order(title asc) {
	 _id,
	title,
	description,
	projectCaption,
	year,
	href,
	quote,
	subtitle,
	slug,
   
  category-> {
					type
	},

  projectImage {
					asset-> {
					url
				}
	}
				
}